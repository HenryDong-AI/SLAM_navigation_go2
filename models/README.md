# Models

Place a small Ultralytics detection model here (for example `yolov8n.pt`) or
set `model_path` in the semantic-mapping configuration. Model files and their
licenses are deliberately not vendored into this repository.
